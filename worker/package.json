{
  "name": "glyphyx-leaderboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute glyphyx-leaderboard --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute glyphyx-leaderboard --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.86.0"
  }
}
